This is a config file to be used with [svgo's command line tool](https://github.com/svg/svgo).

You can ues it with the following command:
```bash
svgo *.svg --config=/PATH-TO-FILE/svgo-config.yml
```